import library from './library';

// * interface to get/set library data

const forms = (() => {
    // data

    // cache DOM

    // bind events

    // factories

    // methods

    return {
        
    }

})();

export default forms;